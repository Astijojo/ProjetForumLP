{% extends 'base.html.twig' %}

{% block content %}
    <div class="container">
        <div class="row">
            {% for message in messages %}
                <article>
                    <p>{{ message.getContenuMess() }}</p>
                </articles>
            {% endfor %}
        </div>
    </div>
{% endblock %}