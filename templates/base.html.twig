<!DOCTYPE html>
<html>
    <head>
        {% block head %}
            <meta charset="UTF-8">
            <title>
                {% block title %}
                    Nom du forum
                {% endblock %}
            </title>
            {% block stylesheets %}
                {# CSS Bootstrap #}
                {{ encore_entry_link_tags('app') }}
            {% endblock %}
        {% endblock %}
    </head>
    <body>
        {% block body %}
            <nav class="navbar navbar-expand-md navbar-dark bg-dark fixed-top">
                <a class="navbar-brand" href="/">Accueil</a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExampleDefault" aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarsExampleDefault">
                    <ul class="navbar-nav mr-auto">
                    </ul>
                    {% if app.user.username is defined %}
                        <button type="button" class="btn btn-dark" disabled>{{ app.user.username }}</button>
                        <a class="btn btn-primary my-2 my-sm-0" href="/logout" role="button"">Se déconnecter</a>
                    {% else %}
                        <button type="button" class="btn btn-dark" disabled>Invité</button>
                        <a class="btn btn-primary my-2 my-sm-0" href="/login" role="button"">Se connecter</a>
                    {% endif %}
                </div>
            </nav>
        {% endblock %}
        <main role="main">
            <div class="jumbotron">
                {% if app.user.username is defined %}
                    <div class="container">
                        <h1 class="display-3">Bienvenue {{ app.user.username }} 🔥</h1>
                    </div>
                {% else %}
                    <div class="container">
                        <h1 class="display-3">Bienvenue jeune thrasher 🔥</h1>
                        <h3>Tu aimes le skate 🛹 et faire du sale ? Viens partager ta passion avec nous !</h3>
                        <p><a class="btn btn-primary btn-lg" href="/register" role="button">S'inscrire &raquo;</a></p>
                    </div>
                {% endif %}
            </div>
            {% block content %}
                {# Enfant #}
            {% endblock %}
        </main>
        {% block footer %}
            <footer class="container">
                <p>&copy; 2020-2021</p>
            </footer>
        {% endblock %}
        {% block javascripts %}
            {# JS Bootstrap #}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </body>
</html>
